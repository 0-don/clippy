# Maintainer: 0-don <https://github.com/0-don>
pkgname={{NAME}}-bin
pkgver={{VERSION}}
pkgrel=1
pkgdesc="{{DESCRIPTION}}"
arch=('x86_64')
url="https://github.com/0-don/{{NAME}}"
license=('{{LICENSE}}')
depends=('libappindicator-gtk3' 'webkit2gtk-4.1' 'gtk3' 'xdotool')
provides=('{{NAME}}')
conflicts=('{{NAME}}')
source=("${pkgname}-${pkgver}.deb::${url}/releases/download/v${pkgver}/{{NAME}}_${pkgver}_amd64.deb")
sha256sums=('{{SHA256}}')

package() {
    bsdtar -xf data.tar.* -C "${pkgdir}/"
}
